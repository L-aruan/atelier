{
  "name": "@atelier/engine-image",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@atelier/types": "workspace:*",
    "browser-image-compression": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}